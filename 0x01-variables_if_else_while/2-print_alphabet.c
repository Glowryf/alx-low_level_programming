/**
 * main - Entry point
 *
 * Description: Prints the last digit of an input.
 *
 * Return: Always 0 (Success)
 */
#include <stdio.h>
#include <stdlib.h>
int main(void)
{
	char a = 'a';

	while (a <= 'z')

	{
		putchar(a);
		a++;
	}

	putchar('\n');
	return (0);
}

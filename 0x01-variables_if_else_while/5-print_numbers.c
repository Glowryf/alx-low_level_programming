/**
 * main - Prints all single digit numbers of base 10 starting from 0.
 *
 * Return: Always 0 (Success)
 *
 */
#include <stdio.h>
int main(void)
{
	int a;

	for (a = 0; a < 10; a++)
		printf("%i", a);
	printf("\n");
	return (0);
}
